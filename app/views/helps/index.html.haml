%h1 Listing helps

%table
  %thead
    %tr
      %th Name
      %th Email
      %th Message
      %th
      %th
      %th

  %tbody
    - @helps.each do |help|
      %tr
        %td= help.name
        %td= help.email
        %td= help.message
        %td= link_to 'Show', help
        %td= link_to 'Edit', edit_help_path(help)
        %td= link_to 'Destroy', help, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Help', new_help_path
